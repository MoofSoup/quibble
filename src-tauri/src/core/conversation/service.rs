fn checkout_thread(){}

//Service.rs contains the business logic that manages and updates conversation data.

//pub fn checkout_thread
    //Clone a thread at the specific id
    //Update that thread's status to "checked out"
//pub fn return_thread
    //update the thread in the instance of the conversationsData struct
    // 
    //update the postgres DB